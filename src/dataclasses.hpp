#pragma once

#include <map>
#include <string>
#include <vector>

/**
 * @brief Stores data pertaining to a community college course.
 *
 */
struct Course
{
    std::string name;
    // prerequisites
    // units
    // ...
};